
# Functional Use Case Inventory

## 1. Intelligence Layer
| ID | Title | Trigger | Expected Outcome |
| :--- | :--- | :--- | :--- |
| UC-INT-01 | Set Commander Intent | User enters strategy in `MasterBrainDashboard`. | `strategy.investmentThesis` updated and saved to `localStorage`. |
| UC-INT-02 | Explode Market Gaps | User clicks "Start Inference" in `NexusPrime`. | 3-5 high-probability opportunities injected into `domains`. |
| UC-INT-03 | Train Logic | User approves/rejects decision in `FeedbackDashboard`. | `trainingData` status updates to "approved" or "rejected". |

## 2. Acquisition Layer
| ID | Title | Trigger | Expected Outcome |
| :--- | :--- | :--- | :--- |
| UC-ACQ-01 | Sniper Sweep | User enters niche in `DiscoveryDashboard`. | Live grounded results displayed with `estimatedPrice` and `justification`. |
| UC-ACQ-02 | Forensic Audit | User clicks "Audit Action" in `EvaluationDashboard`. | `trademarkRisk` and `liquidityScore` populated via Gemini 3 Pro. |
| UC-ACQ-03 | Buy Asset | User clicks "Purchase from Namecheap" in `PurchaseDashboard`. | External tab opens with registrar search; user can mark as "Purchased". |
| UC-ACQ-04 | Drop Capture | User monitors `DropSniperDashboard`. | Domains with `dropDate` identified; "Tactical Intelligence" generated for targets. |

## 3. Operations Layer
| ID | Title | Trigger | Expected Outcome |
| :--- | :--- | :--- | :--- |
| UC-OPS-01 | Flow Sync | User drags domain in `PipelineDashboard`. | `domain.status` updates (e.g., from 'available' to 'purchased'). |
| UC-OPS-02 | Engineer Brand | User clicks "Generate Brand DNA" in `PortfolioManager`. | Tagline and Base64 Logo URL generated and stored in `domain.brandAssets`. |
| UC-OPS-03 | Prove Value | User clicks "Generate Proof" in `ValueProofDashboard`. | Landing page headline, features, and disruption score synthesized. |
| UC-OPS-04 | Multiply Revenue | User triggers "Multiply Value" in `ValueMultiplier`. | Lead-gen revenue model and strategic corporate acquirers harvested. |

## 4. Liquidation Layer
| ID | Title | Trigger | Expected Outcome |
| :--- | : :--- | :--- | :--- |
| UC-LIQ-01 | Optimize Listing | User clicks "Optimize" in `MarketplaceDashboard`. | Suggested Buy Now price and SEO Search Snippets generated for Afternic. |
| UC-LIQ-02 | Executive Pitch | User generates pitch in `MessagingDashboard`. | Professional email draft created and link to Gmail provided. |
| UC-LIQ-03 | Battle Negotiation | User analyzes reply in `NegotiationDashboard`. | "Hidden Motives" revealed and "Tactical Counter-Offer" suggested. |
| UC-LIQ-04 | Auction Radar | User refreshes `AuctionWatchDashboard`. | Heatmap updated with current sector liquidity scores. |
