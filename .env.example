
# Firebase Configuration is handled via firebase-applet-config.json

# Gemini API Key (handled by platform)
# API_KEY=

# Note: Create .env.local file with actual values for local development if needed
# NEVER commit .env.local to git
